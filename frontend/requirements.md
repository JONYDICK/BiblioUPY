## Packages
framer-motion | Page transitions, hover effects, and scroll animations
lucide-react | Icons for UI elements (Book, File, Download, etc.)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
API expects GET /api/resources and POST /api/resources
Language toggle requires global state management (implemented via React Context)
